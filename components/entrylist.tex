\usepackage{fancybox}
\newenvironment{entrylist}
{
  \begingroup
  % % 2mm, the same as \parindent
  % \setlist{leftmargin=*, topsep=0pt}
  % \addtolength{\leftmargini}{2mm}
  % \setlist{topsep=0pt}
  \setlength{\extrarowheight}{20pt}
  \vspace{-20pt}
  % FIXME: does not break page if list is too long
  \begin{tabular}{l}
}
{
  \end{tabular}
  \endgroup
}

\newcommand{\entriesdelimiter}{\\}
\newcommand{\entrydescription}[1]{
  \setlength{\parindent}{4mm}
  \setlength{\parskip}{2mm}
  #1
}
